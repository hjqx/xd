MADE LONG TIME AGO BY ZQH AKA HJQX
```py
def _3(*d)->(_:=lambda*_,:_):g,=*d,;yield(_(_(*(zip([g],['q''h',])))))
f = lambda d, : next(c for c in d)

@lambda *_,: (
    j := ((lambda d, *g: (d))(f ([lambda
            : type or g,])
        )
    )())(
    "G", (), {"_": lambda: print}
)._() ( *(lambda *_,: f(_)) (*_) )

@X := lambda *M: (lambda M,: (K := M))(
    f(M,)
)(_3, '\172')
def V(*_,):d,c,=(c:=(*_,));(yield((*(type('nyaN4',(),{'W':_3,}).W(*c)),)))
```
